[build]
  publish = "."
  command = "npm run build"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["pdf-parse", "docx"]

[[redirects]]
  from = "/api/report"
  to = "/.netlify/functions/report"
  status = 200

